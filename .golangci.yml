version: "2"

run:
  timeout: 5m
  tests: true

linters:
  disable-all: true
  enable:
    - govet
    - staticcheck
    - errcheck
    - ineffassign
    - unused
    - gosec
    - revive
    - nilerr
    - bodyclose
    - unconvert

formatters:
  enable:
    - goimports
    - gofmt